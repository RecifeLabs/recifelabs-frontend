name: Deploy Recife Labs - Estático 🚀

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Deploy via Rsync para a VM Azure
        uses: Burnett01/rsync-deployments@7.0.0
        with:
          # Sincroniza todos os arquivos da raiz do repo para a pasta do Nginx
          switches: -avzr --delete
          path: .
          remote_path: /var/www/recifelabs_html/
          remote_host: ${{ secrets.REMOTE_HOST }}
          remote_user: ${{ secrets.REMOTE_USER }}
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
          # Ignora a verificação manual de Host Key para automação
          rsh: ssh -o StrictHostKeyChecking=no
          # Evita enviar metadados do Git e do próprio workflow para a VM
          args: --exclude=.git* --exclude=.github*

      - name: Notificação de Sucesso
        run: echo "🚀 Deploy finalizado! Verifique em https://www.recifelabs.dev"