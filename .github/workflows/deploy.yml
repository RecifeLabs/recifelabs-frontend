name: Deploy Recife Labs - Estático 🚀

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Deploy via Rsync para a VM Azure
        uses: Burnett01/rsync-deployments@7.0.0
        with:
          # Switches padrão para transferência recursiva e compressão
          switches: -avzr --delete
          path: .
          remote_path: /var/www/recifelabs_html/
          remote_host: ${{ secrets.REMOTE_HOST }}
          remote_user: ${{ secrets.REMOTE_USER }}
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
          # Ajuste no RSH: Removido o 'ssh' inicial para evitar erro de resolução
          rsh: -o StrictHostKeyChecking=no
          # Mantendo a exclusão de arquivos de sistema do Git
          args: --exclude=.git* --exclude=.github*

      - name: Notificação de Sucesso
        run: echo "🚀 Deploy finalizado! Acesse https://www.recifelabs.dev"